# -*- coding: utf-8 -*-
"""
D1 演習課題3（問題版）：地域の特徴を主成分分析で要約しよう
==========================================================

【この課題で行うこと】
24の架空地域には、年収、家賃、通勤時間、緑地、診療所、犯罪という
6つの指標があります。項目が多いと地域同士を比較しにくいため、主成分分析
（PCA）を使い、6項目の特徴をPC1とPC2という2つの総合指標へ要約します。

今回は、PCAのすべてのコードを一から書く必要はありません。標準化とPCAの
重要な部分だけ、選択肢から選んで「____」を埋めます。表の整形やグラフの
描画など、複雑な部分は記入済みのコードを読みながら結果を確認してください。

【用語】
・標準化：単位や数値の大きさが違う項目を、同じ尺度へそろえる処理
・主成分：複数の項目を組み合わせて作る、新しい総合指標
・寄与率：その主成分が、元の情報をどの程度説明できるかを表す割合
・主成分負荷量：元の各指標が、主成分とどの方向にどれほど強く関係するか

【取り組み方】
1. 問題1から順に説明と選択肢を読みます。
2. コード中の「____」を、選んだ文字や単語に置き換えます。
3. 全部埋めた後に実行し、表示された表とグラフを確認します。
4. 最後の確認問題と考察問題に答えます。

注意：「____」が残っている間は、プログラムは正しく実行できません。
"""

import matplotlib.pyplot as plt
import pandas as pd
from sklearn.decomposition import PCA
from sklearn.preprocessing import StandardScaler


# 分析用データは完成済みです。数値は変更せずに使用してください。
# income_10k_yen      ：平均年収（万円）
# rent_1k_yen         ：単身向け平均家賃（千円）
# commute_minutes     ：平均通勤時間（分）
# green_space_percent ：地域面積に占める緑地の割合（%）
# clinics_per_10k     ：人口1万人あたり診療所数
# crimes_per_1k       ：人口千人あたり犯罪認知件数
regions = pd.DataFrame({
    "region": [f"Area-{number:02d}" for number in range(1, 25)],
    "income_10k_yen": [
        610, 580, 550, 520, 490, 470, 450, 430,
        510, 480, 460, 440, 420, 400, 390, 370,
        460, 440, 410, 390, 370, 350, 340, 320,
    ],
    "rent_1k_yen": [
        132, 125, 118, 110, 105, 98, 92, 88,
        102, 96, 90, 85, 80, 76, 72, 68,
        82, 78, 73, 69, 64, 60, 57, 53,
    ],
    "commute_minutes": [
        48, 45, 43, 41, 39, 38, 36, 35,
        40, 38, 36, 34, 32, 31, 29, 28,
        34, 32, 30, 28, 26, 24, 23, 21,
    ],
    "green_space_percent": [
        8, 10, 12, 15, 18, 20, 22, 25,
        14, 17, 21, 24, 28, 31, 34, 37,
        23, 27, 32, 36, 40, 44, 48, 52,
    ],
    "clinics_per_10k": [
        8.8, 8.4, 8.1, 7.8, 7.5, 7.3, 7.0, 6.8,
        9.6, 9.2, 8.9, 8.5, 8.2, 7.9, 7.6, 7.4,
        7.1, 7.4, 7.7, 8.0, 8.3, 8.6, 8.9, 9.2,
    ],
    "crimes_per_1k": [
        12.5, 11.8, 11.0, 10.2, 9.7, 9.0, 8.5, 8.0,
        9.8, 9.2, 8.7, 8.1, 7.5, 7.0, 6.6, 6.2,
        7.8, 7.2, 6.7, 6.1, 5.6, 5.1, 4.7, 4.3,
    ],
})

print("【地域データ：先頭5行】")
print(regions.head())


# ==================================================================
# 問題1：PCAで計算する列だけを取り出そう
# ==================================================================
# region列は地域名を表す文字列なので、数値計算には使いません。
# drop()でregion列を除き、残った6つの数値列をXへ代入します。
#
# 選択肢：A. "region"    B. "PC1"    C. "income_10k_yen"
# 自分の答え：
X = regions.drop(columns=____)

print("\n【各指標の平均と標準偏差】")
print(X.agg(["mean", "std"]).round(2))


# 【確認問題1】region列をPCAから除く理由はどれですか。
# A. 地域名は量的な特徴を表す数値ではないから
# B. PCAでは列を5個までしか使えないから
# C. 地域名がアルファベットだから
# 自分の答え：


# ==================================================================
# 問題2：6項目を標準化しよう
# ==================================================================
# 年収は数百、診療所数は一桁というように、項目によって単位と数値の大きさが
# 違います。そのままPCAを行うと、数値が大きい項目の影響が強くなりやすいため、
# StandardScalerを使って同じ尺度へそろえます。
#
# 問題2-A：「____」へ入るクラス名を選んでください。
# 選択肢：A. PCA    B. StandardScaler    C. DataFrame
# 自分の答え：
scaler = ____()

# 問題2-B：標準化の計算と変換を続けて行うメソッドを選んでください。
# 選択肢：A. fit_transform    B. drop    C. insert
# 自分の答え：
X_scaled = scaler.____(X)


# 【確認問題2】PCAの前に標準化する主な理由はどれですか。
# A. 地域数を増やすため
# B. 単位の異なる6項目を公平に比較するため
# C. 地域名を自動で作るため
# 自分の答え：


# ==================================================================
# 問題3：6項目を2つの主成分へ要約しよう
# ==================================================================
# n_componentsは、いくつの主成分へ要約するかを指定する値です。
# 今回はPC1とPC2の2つに要約します。
#
# 問題3-A：「____」へ入る数を選んでください。
# 選択肢：A. 2    B. 6    C. 24
# 自分の答え：
pca = PCA(n_components=____)

# 問題3-B：PCAを学習し、各地域をPC1・PC2の値へ変換します。
# 選択肢：A. value_counts    B. fit_transform    C. rename
# 自分の答え：
principal_components = pca.____(X_scaled)


# ==================================================================
# 準備：PCAの変換結果を読みやすい表にしよう
# ==================================================================
# この部分は記入済みです。24地域の変換結果へPC1、PC2という列名を付け、
# 先頭に地域名を追加します。
pca_result = pd.DataFrame(
    principal_components,
    columns=["PC1", "PC2"],
)
pca_result.insert(0, "region", regions["region"])

print("\n【PCA変換後の先頭5地域】")
print(pca_result.head().round(3))


# ==================================================================
# 問題4：寄与率を確認しよう
# ==================================================================
# 寄与率は、各主成分が元の6項目の情報をどれくらい説明しているかを表します。
# scikit-learnのPCAでは、学習後の寄与率が特定の属性に保存されています。
#
# 選択肢：A. target_names    B. explained_variance_ratio_
#         C. principal_components
# 自分の答え：
explained = pca.____

print("\n【主成分の寄与率】")
print(f"第1主成分（PC1）: {explained[0]:.1%}")
print(f"第2主成分（PC2）: {explained[1]:.1%}")
print(f"2主成分の累積寄与率: {explained.sum():.1%}")


# 【確認問題3】PC1の寄与率が70%だった場合、正しい説明はどれですか。
# A. 70%の地域だけを分析した
# B. PC1が元データの情報や変動のおよそ70%を説明している
# C. 品種の予測正解率が70%だった
# 自分の答え：


# ==================================================================
# 問題5：各指標が主成分へ与える影響を調べよう
# ==================================================================
# pca.components_には主成分負荷量が入っています。.Tで表を転置すると、
# 行が6指標、列がPC1・PC2となり、読みやすくなります。
#
# 選択肢：A. components_.T    B. explained_variance_ratio_
#         C. target_names
# 自分の答え：
loadings = pd.DataFrame(
    pca.____,
    index=X.columns,
    columns=["PC1", "PC2"],
)

print("\n【主成分負荷量：絶対値が大きいほど影響が強い】")
print(loadings.round(3))


# 各主成分に強く影響する上位3項目を探すコードです。
# abs()で正負を無視し、nlargest(3)で大きい順に3項目を選びます。
for component in ["PC1", "PC2"]:
    important_features = loadings[component].abs().nlargest(3).index.tolist()
    print(f"{component}に強く影響する項目: {', '.join(important_features)}")


# 【確認問題4】負荷量について正しい説明はどれですか。
# A. 絶対値が大きい指標ほど、その主成分との関係が強い
# B. 必ず正の値でなければならない
# C. 単位はすべて万円である
# 自分の答え：


# ==================================================================
# 準備：地域と各指標の関係をグラフで確認しよう
# ==================================================================
# 描画コードは複雑なので記入済みです。青い点は各地域、赤い矢印は各指標が
# 増加する方向を表します。矢印と同じ方向にある地域ほど、その指標が平均より
# 高い傾向があります。反対方向なら、平均より低い傾向があります。
fig, ax = plt.subplots(figsize=(11, 7))
ax.scatter(pca_result["PC1"], pca_result["PC2"],
           color="#4C78A8", s=75, alpha=0.8)

for _, row in pca_result.iterrows():
    ax.annotate(row["region"], (row["PC1"], row["PC2"]),
                xytext=(5, 4), textcoords="offset points", fontsize=8)

scale = 3.2
for feature in X.columns:
    x_direction = loadings.loc[feature, "PC1"] * scale
    y_direction = loadings.loc[feature, "PC2"] * scale
    ax.arrow(0, 0, x_direction, y_direction, color="#E45756",
             alpha=0.75, head_width=0.08, length_includes_head=True)
    ax.text(x_direction * 1.12, y_direction * 1.12, feature,
            color="#B33A3A", fontsize=8, ha="center")

ax.axhline(0, color="gray", linewidth=0.8)
ax.axvline(0, color="gray", linewidth=0.8)
ax.set_xlabel(f"PC1 ({explained[0]:.1%} explained)")
ax.set_ylabel(f"PC2 ({explained[1]:.1%} explained)")
ax.set_title("Regional Characteristics Summarized by PCA")
ax.grid(alpha=0.2)
fig.tight_layout()
plt.show()


# ==================================================================
# グラフの読み取り問題
# ==================================================================
# 問題6：矢印と地域の位置について、正しい説明を選んでください。
# A. 矢印と同じ方向の地域ほど、その指標が平均より高い傾向がある
# B. 矢印と反対方向の地域ほど、その指標が必ず最大になる
# C. 矢印の長さは地域の人口を表している
# 自分の答え：


# 問題7：PC1やPC2の値について、正しい説明を選んでください。
# A. 正なら良い地域、負なら悪い地域である
# B. 正負は軸の方向を表すだけで、それ自体に良い・悪いの意味はない
# C. 負の地域は分析から除かなければならない
# 自分の答え：


# ==================================================================
# 最後の考察
# ==================================================================
# 1. PC1はどのような地域特性を表す軸だと考えられますか。
#    PC1の負荷量の絶対値が大きい項目を根拠に説明してください。
# 自分の答え：


# 2. 「家賃が安く、緑が多い地域」を探す場合、グラフのどの方向を
#    見ればよいでしょうか。rentとgreen_spaceの矢印を使って説明してください。
# 自分の答え：


# 3. この結果だけで移住先を決めてよいでしょうか。人口構成、仕事、交通、
#    災害リスクなど、追加で確認したい情報を2つ以上挙げてください。
# 自分の答え：
