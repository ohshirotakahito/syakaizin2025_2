# -*- coding: utf-8 -*-
"""
演習：採点の最も高いものと最も低いものを除いて平均をとる

採点は長さ6のリストで与えられる。最高得点と最低得点を1つずつ除いた
残り4個の平均点を求める。
"""

# 得点を求めるリスト
a = [3.0, 4.0, 5.0, 4.4, 3.2, 5.5]
b = [4.0, 4.2, 5.0, 4.8, 2.2, 3.8]
c = [1.0, 3.0, 3.2, 6.4, 4.2, 3.5]

# 関数を使わない場合
# TODO: aの最高点と最低点を除いた平均を計算して表示してください
# ヒント： (sum(a) - max(a) - min(a)) / (len(a) - 2)


# 関数を使う場合
def score(x):
    """最高点と最低点を除いた平均点を返す。"""
    # TODO: ここに実装してください
    pass


print(score(a))
print(score(b))
print(score(c))
